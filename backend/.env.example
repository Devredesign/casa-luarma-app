# Backend (Express)
# Copiá este archivo a `backend/.env` y completá los valores.

PORT=5000

# MongoDB Atlas
MONGODB_URI=

# OpenAI (para /api/chat)
OPENAI_API_KEY=

# CORS (opcional): lista separada por comas
# Ej: https://casa-luarma-app.vercel.app,http://localhost:3000
ALLOWED_ORIGINS=
